# website

Landing page at reflection.network.

## Stack

Astro 5, TypeScript (strict).

## Deployment

GitHub Pages, triggered on push to `main`. Build: `npm ci && npm run build`, output in `./dist`.

## Scripts

`npm run dev`, `npm run build`, `npm run preview`.
